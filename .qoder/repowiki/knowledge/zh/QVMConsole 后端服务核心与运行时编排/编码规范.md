- 子包通过 `deps.go` 定义镜像类型或 Hook 变量，避免直接引用根包或其他兄弟包类型以消除循环依赖。
- 长耗时业务逻辑（如 VM 克隆、迁移）必须封装为 `taskqueue` 任务处理器，并通过 `progress` 回调函数上报执行进度。
- 服务启动时在 `main.go` 中显式调用各类 `Ensure...` 或 `Restore...` 函数，以同步数据库持久化状态与底层系统（如 OVS, iptables）的运行态。