- **一体化构建**：使用根目录的 `build.sh` 脚本串联前端 `npm run build` 与后端 `go build`，最终打包为包含 `web-dist` 静态资源的单一发行包。
- **开发热重载**：后端通过 `.air.toml` 配置 `air` 工具实现代码修改后的自动编译与重启。
- **持久化配置同步**：启动时优先从数据库加载系统设置覆盖环境变量，并通过 `config.ValidateSecurity()` 强制校验安全基线。