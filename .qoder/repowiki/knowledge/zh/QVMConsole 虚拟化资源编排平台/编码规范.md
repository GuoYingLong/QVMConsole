- 跨包依赖通过定义 `Deps` 结构体并在 `init()` 函数中完成注入，禁止直接 import 产生循环引用的业务包。
- 长耗时业务逻辑（如 VM 克隆、磁盘迁移）必须封装为 `taskqueue.RegisterHandler` 处理器，通过 SSE 或轮询向前端推送进度。
- 子包若需调用根包未导出的逻辑，须通过 `service/hooks.go` 中定义的全局函数指针变量进行反向注册。
- API 路由按功能模块分组（如 `/vm`, `/network`, `/vpc`），并统一在 `router.Setup()` 中挂载鉴权与限流中间件。